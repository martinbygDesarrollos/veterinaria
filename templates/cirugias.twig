{% extends "base.twig" %}
{% block head %}
{% endblock %}
{% block content %}
<div class="d-flex justify-content-center">
	<div class="card border-top-0 rounded-bottom border shadow w-75">
		<div class="card-header d-flex justify-content-between bg-mycolor">
			<h5 class="card-title">Cirugías</h5><div><input type="date" name="" id="idInputTodayCalendar" onchange="getCirugiasByDay(this.value)">
			<button class="btn btn-dark" data-toggle="tooltip" data-placement="top" title="Agregar nuevo socio" onclick="newRowCirugiaCalendar()"><i class="fas fa-calendar-plus"></i></button></div>
		</div>
		<div class="card-body">
			<div id="containerTable">
				<table class="" style="width: 100%;">
					<tbody id="tbodyCirugiasCalendar"></tbody>
				</table>
			</div>
		</div>
	</div>
</div>
{%block script%}
<script type="text/javascript" src="{{path_for('Inicio')}}scripts/utils/utils.js?{{version}}"></script>
<script type="text/javascript" src="{{path_for('Inicio')}}scripts/agenda.js?{{version}}"></script>
<script type="text/javascript" src="{{path_for('Inicio')}}scripts/utils/sendPost.js?{{version}}"></script>

<script type="text/javascript">
	console.log("document ready cirugias.twig")
	console.log(getCurrentDate());
	$("#idInputTodayCalendar").val(getCurrentDate());
	getCirugiasByDay( $("#idInputTodayCalendar").val() );
</script>
{% endblock %}
{% endblock %}